{
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@prisma/client": "^6.13.0",
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "nodemon": "^3.1.10",
    "prisma": "^6.13.0",
    "typescript": "^5.9.2",
    "ts-node-dev": "^2.0.0"

  },
  "scripts": {
    "start": "node server",
    "dev": "prisma generate && ts-node-dev --respawn --transpile-only main.ts"

  }
}
